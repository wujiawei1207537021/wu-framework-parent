<?xml version="1.0" encoding="UTF-8"?>
<project xmlns="http://maven.apache.org/POM/4.0.0"
         xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
         xsi:schemaLocation="http://maven.apache.org/POM/4.0.0 http://maven.apache.org/xsd/maven-4.0.0.xsd">
    <!--    <parent>-->
    <!--        <groupId>org.springframework.boot</groupId>-->
    <!--        <artifactId>spring-boot-starter-parent</artifactId>-->
    <!--        <version>2.2.5.RELEASE</version>-->
    <!--    </parent>-->

    <modelVersion>4.0.0</modelVersion>

    <groupId>com.wu</groupId>
    <artifactId>wu-framework-parent</artifactId>
    <version>1.0.0</version>
    <packaging>pom</packaging>
    <description>系统框架</description>
    <properties>

        <java.version>1.8</java.version>
        <maven-clean-plugin.version>3.1.0</maven-clean-plugin.version>
        <maven-compiler-plugin.version>3.8.0</maven-compiler-plugin.version>
        <maven-dependency-plugin.version>3.1.1</maven-dependency-plugin.version>
        <maven-install-plugin.version>2.5.2</maven-install-plugin.version>
        <maven-invoker-plugin.version>3.1.0</maven-invoker-plugin.version>
        <maven-jar-plugin.version>3.1.1</maven-jar-plugin.version>
        <maven-resources-plugin.version>3.1.0</maven-resources-plugin.version>
        <maven-shade-plugin.version>3.2.1</maven-shade-plugin.version>
        <maven-surefire-plugin.version>2.22.1</maven-surefire-plugin.version>

        <wu-db-starter.version>1.0.1</wu-db-starter.version>
        <wu-redis-starter.version>1.0.0</wu-redis-starter.version>
        <wu-log-starter.version>1.0.0</wu-log-starter.version>
        <wu-swagger-starter.version>1.0.1</wu-swagger-starter.version>
        <wu-file-starter.version>1.0.1</wu-file-starter.version>

        <spring-boot-starter.version>2.2.5.RELEASE</spring-boot-starter.version>
    </properties>

    <modules>
        <module>wu-framework-shiro</module>
        <!--        <module>wu-framework-system</module>-->
        <module>wu-inner-intergration</module>
        <module>wu-freamwork-test</module>
        <module>wu-inner-interface</module>
        <module>wu-smart-intergration</module>
        <module>wu-framework-plus</module>
        <module>wu-easy-parent</module>
        <!--        <module>wu-common-transform</module>-->
    </modules>

    <dependencies>
        <!--    lombok    -->
        <dependency>
            <groupId>org.projectlombok</groupId>
            <artifactId>lombok</artifactId>
        </dependency>
        <!-- 生成 spring-configuration-metadata.json-->
        <dependency>
            <groupId>org.springframework.boot</groupId>
            <artifactId>spring-boot-configuration-processor</artifactId>
            <optional>true</optional>
        </dependency>
    </dependencies>

    <dependencyManagement>
        <dependencies>
            <dependency>
                <!-- Import dependency management from Spring Boot -->
                <groupId>org.springframework.boot</groupId>
                <artifactId>spring-boot-dependencies</artifactId>
                <version>${spring-boot-starter.version}</version>
                <type>pom</type>
                <scope>import</scope>
            </dependency>

            <!--数据库-->
            <dependency>
                <groupId>com.wu</groupId>
                <artifactId>wu-db-starter</artifactId>
                <version>${wu-db-starter.version}</version>
            </dependency>
            <!--swagger-->
            <dependency>
                <groupId>com.wu</groupId>
                <artifactId>wu-swagger-starter</artifactId>
                <version>${wu-swagger-starter.version}</version>
            </dependency>
            <!--redis-->
            <dependency>
                <groupId>com.wu</groupId>
                <artifactId>wu-redis-starter</artifactId>
                <version>${wu-redis-starter.version}</version>
            </dependency>
            <!--文件资源服务-->
            <dependency>
                <groupId>com.wu</groupId>
                <artifactId>wu-file-starter</artifactId>
                <version>${wu-file-starter.version}</version>
            </dependency>
            <!--      日志      -->
            <dependency>
                <groupId>com.wu</groupId>
                <artifactId>wu-log-starter</artifactId>
                <version>${wu-log-starter.version}</version>
            </dependency>
        </dependencies>
    </dependencyManagement>

    <distributionManagement>
        <repository>
            <id>releases</id>
            <url>http://192.168.1.86:9081/repository/maven-releases/</url>
        </repository>
        <snapshotRepository>
            <id>snapshots</id>
            <url>http://192.168.1.86:9081/repository/maven-snapshots/</url>
        </snapshotRepository>
    </distributionManagement>
    <build>
        <pluginManagement>
            <plugins>
                <plugin>
                    <groupId>org.springframework.boot</groupId>
                    <artifactId>spring-boot-maven-plugin</artifactId>
                    <version>${spring-boot-starter.version}</version>
                    <executions>
                        <execution>
                            <id>repackage</id>
                            <goals>
                                <goal>repackage</goal>
                            </goals>
                        </execution>
                    </executions>
                </plugin>
                <plugin>
                    <artifactId>maven-jar-plugin</artifactId>
                    <version>${maven-jar-plugin.version}</version>
                </plugin>
                <plugin>
                    <artifactId>maven-clean-plugin</artifactId>
                    <version>${maven-clean-plugin.version}</version>
                </plugin>
                <plugin>
                    <artifactId>maven-resources-plugin</artifactId>
                    <version>${maven-resources-plugin.version}</version>
                </plugin>
                <plugin>
                    <artifactId>maven-compiler-plugin</artifactId>
                    <version>${maven-compiler-plugin.version}</version>
                    <configuration>
                        <source>1.8</source>
                        <target>1.8</target>
                        <compilerArgs>
                            <compilerArg>-parameters</compilerArg>
                        </compilerArgs>
                    </configuration>
                </plugin>
                <plugin>
                    <artifactId>maven-surefire-plugin</artifactId>
                    <version>${maven-surefire-plugin.version}</version>
                </plugin>
            </plugins>
        </pluginManagement>
    </build>


</project>